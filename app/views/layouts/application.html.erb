<!DOCTYPE html>
<html>

  <head>
	
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <title>


<%= content_for?(:title) ? yield(:title) : "THIMUN Qatar Online Registration System" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "THIMUN Qatar Online Registration System" %>">
    <%= stylesheet_link_tag "application", :media => "all" %>
    <%= javascript_include_tag "application" %>
    <%= csrf_meta_tags %>
    <%= yield(:head) %>


		

  </head>
  <body>
	
    <header class="navbar navbar-fixed-top">
      <nav class="navbar-inner">
        <div class="container">
          <%= render 'layouts/navigation' %>
	

        </div>
      </nav>
    </header>
    <div class="container" style="background: white; padding-left:20px; padding-right:20px;">
      <%= render 'layouts/messages' %>
      <%= yield %>

    </div>

  </body>
</html>
<%= form_for(@country_list) do |f| %>
  <% if @country_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@country_list.errors.count, "error") %> prohibited this country_list from being saved:</h2>

      <ul>
      <% @country_list.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :country_id %>
    <%= f.number_field :country_id %>
  </div>
  <div class="field">
    <%= f.label :list_id %>
    <%= f.number_field :list_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

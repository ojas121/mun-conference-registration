<%= form_for(@committee_school) do |f| %>
  <% if @committee_school.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@committee_school.errors.count, "error") %> prohibited this committee_school from being saved:</h2>

      <ul>
      <% @committee_school.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :committee_id %><br />
    <%= f.number_field :committee_id %>
  </div>
  <div class="field">
    <%= f.label :school_id %><br />
    <%= f.number_field :school_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

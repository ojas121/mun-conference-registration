<%= form_for(@committee) do |f| %>
  <% if @committee.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@committee.errors.count, "error") %> prohibited this committee from being saved:</h2>

      <ul>
      <% @committee.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :committee_name %>
    <%= f.text_field :committee_name %>
  </div>
  <div class="field">
    <%= f.label :Number_of_delegates %>
    <%= f.number_field :num_delegates %>
  </div>
  <div class="field">
    <%= f.label :list_id %>
    <%= f.collection_select :list_id, List.all, :id, :list_name %>
  </div>
  <div class="field">
    <%= f.label :conference_id %>
    <%= f.collection_select :conference_id, Conference.all, :id, :conference_title %>
  </div>

</br></br>

<div class="actions">
	<button type="submit" class="btn">Create committee</button>
  </div>


<% end %>

<%= form_for(@country_school) do |f| %>
  <% if @country_school.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@country_school.errors.count, "error") %> prohibited this country_school from being saved:</h2>

      <ul>
      <% @country_school.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :country_id %><br />
    <%= f.collection_select :country_id, Country.all, :id, :country_name %>
  </div>

  <div class="field">
    <%= f.label :school_id %><br />
    <%= f.collection_select :school_id, School.all, :id, :school_name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
